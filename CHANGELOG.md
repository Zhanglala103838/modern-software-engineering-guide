# Changelog

本规范是**活制品**——用真实代码库当反例库持续证伪、迭代。
以下记录均为脱敏后的泛化结论,不含任何具体项目/业务信息。

## v0.4 — 2026-06-29

来自一个真实 **老栈系统(FastAdmin/ThinkPHP5,无 CI、生产直改、无自动化测试、千行级 god 文件)** 的实战检验。补上了规范缺的最后一块——**棕地适配**:

- **新增第八部分「棕地 / 遗留系统适配」**。核心洞察:前七部分大量假设现代栈,对遗留系统直接套用 = 规范自己警告的"硬推带来挫败感"。新章节按"风险下降/投入"排序最小安全网,绝不靠重写:
  - 先建**可复现+可回滚**(VCS 即部署真相,改完即提交,废除 .bak 手工备份)——最高 ROI。
  - 改遗留代码前先打**特征化测试**(边界快照锁现状),而非要求先有单测。
  - god 文件用 **strangler 绞杀**增量替换,禁大爆炸重写。
  - no-CI 流最小门禁:语法 lint + 端点 smoke + 先提交后部署。
  - 锁依赖版本(禁 dev-master/latest)。
  - 廉价可追溯:conventional commit + 工单号 + 关键边界 inline 注释。
- **第七部分加适用性边界**:遗留系统别从 fitness functions 开始,先走第八部分。
- 核心排序原则:遗留系统头号风险常常**不是"没单测",而是"无可复现/无回滚/无审计"**——先解决后者。

> 与 v0.3 的 GDP(治理天花板)形成完整光谱:从"生产直改无 git 真相"到"25 个可执行架构守卫"。规范现在覆盖了**绿地严格态 ↔ 棕地最小安全网**两端。

## v0.3 — 2026-06-29

来自一个真实 **元数据驱动低代码平台(三层架构 + 自演进治理 harness)** 的实战检验(匿名,3 个并行专项审计)。这是迄今信息量最大的一轮:

- **新增第七部分「架构治理即代码(Fitness Functions)」**——本轮最大收获,原规范完全缺失:
  - 架构不变量(层边界 / dispatcher 体积)用**可执行守卫**在提交时强制,非靠 review。
  - **SSOT 用扫描器守**:权威源配静态检测,扫"平行真相"常量。
  - **风险路由 review 深度**:复杂度×风险×不确定性×副作用 打分定 review 权重(细化第四部分)。
  - **高风险变更随附可执行验收脚本** + 防腐警示(一次性脚本会腐烂成 stale gate,需登记维护)。
  - **缺陷复发→沉淀成守卫**:同类缺陷 N 次即固化为 fitness function。
- **🔁 自我纠错:修正 v0.2 的"600–800 行阈值"**。实战证明该阈值太粗——接口封死的 handler / 薄配置 UI 即使上千行也低风险。判据改为**责任爆炸半径**(import 扇出 / 能否独立理解 / 会否长进相邻域),区分 bounded-large(可留)与 tangled-large(必拆)。
- **3.2 部署性补强**:迁移 ordered + idempotent + N-1 兼容 + forward-only rollback;**CD 必须跑与本地同款门禁 + 构建前校验 env**(CD 跳过测试 = 未测代码直接上线)。
- **1.4 测试金字塔补**:比例当可追踪指标;关键用户旅程 e2e 不可趋近 0。

> 元收获:这一轮规范**自己纠正了自己上一版的规则**——正是元原则"规范是可迭代、可反馈的活制品"的实证。

## v0.2 — 2026-06-29

来自一个真实 **Next.js solo / 单部署目标项目** 的实战检验(匿名),新增 4 条:

- **1.2 新增**:错误不许静默吞掉——catch 必须上报 sink / 重抛 / 显式注释,并用**静态门禁**强制(实战中"静默吞错静态扫描脚本"被证明有效)。
- **1.2 新增例外**:solo / 无团队 PR 流的小项目,**强制本地 pre-commit `check`** 可替代 PR CI。
- **2.1 新增可操作阈值**:单文件 ~600–800 行(尤其 UI 组件)即复杂度告警信号(原条目缺少 review 抓手)。
- **3.1 新增**:安全/权限边界必须有**专门边界测试**(越权、数据隔离),不能只测 happy path。
- **3.2 新增例外**:单部署目标项目 feature flag 非必须,env 门控 + 可回滚部署即满足"部署 ≠ 发布"实质。

> 方法论:每个真实项目产出"规范的哪条站不住 / 缺什么 / 需要什么例外",而非"项目好不好"。原始审计留私有,只回注泛化规则。

## v0.1 — 2026-06-29

- 基线版本:基于 David Farley《Modern Software Engineering》的开发 + Review 规范。
- 含完整 SPEC、CONTRIBUTING、PR 模板、CC BY 4.0。
