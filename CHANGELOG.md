# Changelog

本规范是**活制品**——用真实代码库当反例库持续证伪、迭代。
以下记录均为脱敏后的泛化结论,不含任何具体项目/业务信息。

## v0.8 — 2026-06-29

OSS 正向标杆第二批,补强规范三处一直偏薄的维度:

- **可观测性具体化**(参考 **OpenTelemetry**):3.2 从"有日志/指标/告警"升级为——三类信号(trace/metric/log)齐全、跨服务传播 trace context、统一语义约定与资源标签、优先自动埋点 + 按环境采样;点明"生产是最慢但最真实的反馈层"。
- **Feature flag 做实**(参考 **OpenFeature**):3.2 flag 条目补——渐进放量/canary + kill switch、目标投放走 evaluation context(非硬编码)、**flag 是技术债必须有 owner+到期日并清理**、用厂商中立抽象隔离 SDK。
- **集成测试做实**(参考 **Testcontainers**):1.4 新增——集成测试对真实依赖用即弃容器,而非 mock/共享测试库;直接治本本规范多次踩到的"测试间串台"与"mock 假信心",要求本地/CI 同款 + 自动清理。

> 这批把"可观测性""flag""集成测试"从一句话原则升级为有工具锚点的具体做法;flag 的"stale flag = 技术债要清理"和集成测试的"即弃容器代替共享库"是最高价值的两条。

## v0.7 — 2026-06-29

**方法论扩展**:从"用私有项目学反模式(脱敏)"扩到"**用公开 OSS 标杆学正向最佳实践(可具名)**"。第一批 3 个标杆,把规范前沿的几条从"原则"升级为"有成熟工具背书的具体做法":

- **第七部分 fitness functions 具体化**(参考 **ArchUnit**):补可写成测试的不变量类型——包/模块边界、**循环依赖检测**、命名约定、`@Internal` 内部 API 访问控制;点明"架构是活契约不是文档"。
- **第九部分新增消费者驱动契约测试**(参考 **Pact**):服务间 API/消息用 contract-by-example + broker + provider 独立验证 + **can-i-deploy** 发布门禁(区别于共享 DB 的列假设契约)。
- **第九部分迁移具体化**(参考 **strong_migrations**):危险操作→安全替代清单——删列前代码层先忽略再删、改类型/加约束分两步、并发建索引、backfill 分批且在迁移外、生产规模数据演练。

> 这批的价值:本地项目偏"反模式 + 棕地",OSS 标杆补上"绿地正向做法 + 成熟工具锚点",且公开项目可直接具名引用,规范从"我们踩过的坑"升级为"业界验证过的做法 + 我们踩过的坑"。

## v0.6 — 2026-06-29

来自一个真实 **多语言后端 monorepo(PHP + Java 共用一个 MySQL 库,4 个独立 git 子仓 + 各自 CI)** 的深审(3 并行 agent)。补上规范完全没碰过的维度——**跨服务 / 共享数据**:

- **新增第九部分「跨服务与共享数据(多服务/多语言/分仓)」**:
  - 共享数据库 = 隐式契约,要显式化(表所有权登记 + schema 当版本化契约,改共享表需消费方签字)。
  - 跨服务 schema 变更不可原子(分仓分 CI)→ expand-contract 分步走,禁破坏性重命名一把梭。
  - 共享数据上做契约测试(类型/精度/枚举/时区两边假设都验)。
  - 必须一起变的东西要有跨仓变更编排(迁移单一真相目录 + CI 跨语言兼容门禁)。
  - 一个服务只用一套数据访问策略(双 ORM = 两份 schema 映射真相)。
- **3.2 新增密钥不入源码**:env/vault + 扫描器 + 定期审计 allowlist。真实踩坑:仓库装了 secrets 扫描却仍泄露明文 DB 密码 / 云 AK/SK = 扫描器有盲区的虚假安全。

> 这一轮验证:依赖治理(BOM/lock)在多语言项目里反而做得好;但**共库的跨语言契约是最大盲区**——schema 是隐式的、分仓让变更无法原子、零契约测试。同时复用了 v0.3 的"god 文件=责任爆炸半径"(PHP MiniOrder 3676 行 / Java 服务 835 行 18 方法)。

## v0.5 — 2026-06-29

来自一次 **~45 个真实仓库的全量 portfolio 扫描**(6 个并行 agent)。单点深审看不到的**跨项目普遍模式**浮现,回注 5 条:

- **1.2 工作区卫生**:未提交漂移是 portfolio 级普遍病(多仓 150–659 个文件未提交),不止遗留系统——新增"工作区保持近零,大量长期未提交是头号卫生告警"。
- **1.2 测试门禁两种失败模式**:① 有测试但无 CI/pre-commit 强制 → 测试腐烂;② 有 CI 但不 gate 测试(只 build)= CI 剧场。两者都点名。
- **3.1 每栈测试底线**:"这个栈难测"(小程序/原生/桌面)通常是**工具链没搭**,不是不能测;每栈指定框架 + 最低底线。
- **3.2 依赖可复现**:lock 锁精确版本、manifest 用范围;禁 dev-master/SNAPSHOT/latest 进生产(把 v0.4 遗留规则升级为通用)。
- **2.1 复杂度判断排除生成物**:几十万字符多是 minified/生成物,非 god 文件(真实假阳性);且产物/.bak/构建输出不该进 VCS。

> 这一轮的价值:**广度扫描验证了深度审计的结论确实普适**(god 文件、未提交、依赖漂移到处都是),并新增了只有"看全貌"才看得到的卫生类规则。配套新增 README「规范成长光谱」体检表。

## v0.4 — 2026-06-29

来自一个真实 **老栈系统(FastAdmin/ThinkPHP5,无 CI、生产直改、无自动化测试、千行级 god 文件)** 的实战检验。补上了规范缺的最后一块——**棕地适配**:

- **新增第八部分「棕地 / 遗留系统适配」**。核心洞察:前七部分大量假设现代栈,对遗留系统直接套用 = 规范自己警告的"硬推带来挫败感"。新章节按"风险下降/投入"排序最小安全网,绝不靠重写:
  - 先建**可复现+可回滚**(VCS 即部署真相,改完即提交,废除 .bak 手工备份)——最高 ROI。
  - 改遗留代码前先打**特征化测试**(边界快照锁现状),而非要求先有单测。
  - god 文件用 **strangler 绞杀**增量替换,禁大爆炸重写。
  - no-CI 流最小门禁:语法 lint + 端点 smoke + 先提交后部署。
  - 锁依赖版本(禁 dev-master/latest)。
  - 廉价可追溯:conventional commit + 工单号 + 关键边界 inline 注释。
- **第七部分加适用性边界**:遗留系统别从 fitness functions 开始,先走第八部分。
- 核心排序原则:遗留系统头号风险常常**不是"没单测",而是"无可复现/无回滚/无审计"**——先解决后者。

> 与 v0.3 的 GDP(治理天花板)形成完整光谱:从"生产直改无 git 真相"到"25 个可执行架构守卫"。规范现在覆盖了**绿地严格态 ↔ 棕地最小安全网**两端。

## v0.3 — 2026-06-29

来自一个真实 **元数据驱动低代码平台(三层架构 + 自演进治理 harness)** 的实战检验(匿名,3 个并行专项审计)。这是迄今信息量最大的一轮:

- **新增第七部分「架构治理即代码(Fitness Functions)」**——本轮最大收获,原规范完全缺失:
  - 架构不变量(层边界 / dispatcher 体积)用**可执行守卫**在提交时强制,非靠 review。
  - **SSOT 用扫描器守**:权威源配静态检测,扫"平行真相"常量。
  - **风险路由 review 深度**:复杂度×风险×不确定性×副作用 打分定 review 权重(细化第四部分)。
  - **高风险变更随附可执行验收脚本** + 防腐警示(一次性脚本会腐烂成 stale gate,需登记维护)。
  - **缺陷复发→沉淀成守卫**:同类缺陷 N 次即固化为 fitness function。
- **🔁 自我纠错:修正 v0.2 的"600–800 行阈值"**。实战证明该阈值太粗——接口封死的 handler / 薄配置 UI 即使上千行也低风险。判据改为**责任爆炸半径**(import 扇出 / 能否独立理解 / 会否长进相邻域),区分 bounded-large(可留)与 tangled-large(必拆)。
- **3.2 部署性补强**:迁移 ordered + idempotent + N-1 兼容 + forward-only rollback;**CD 必须跑与本地同款门禁 + 构建前校验 env**(CD 跳过测试 = 未测代码直接上线)。
- **1.4 测试金字塔补**:比例当可追踪指标;关键用户旅程 e2e 不可趋近 0。

> 元收获:这一轮规范**自己纠正了自己上一版的规则**——正是元原则"规范是可迭代、可反馈的活制品"的实证。

## v0.2 — 2026-06-29

来自一个真实 **Next.js solo / 单部署目标项目** 的实战检验(匿名),新增 4 条:

- **1.2 新增**:错误不许静默吞掉——catch 必须上报 sink / 重抛 / 显式注释,并用**静态门禁**强制(实战中"静默吞错静态扫描脚本"被证明有效)。
- **1.2 新增例外**:solo / 无团队 PR 流的小项目,**强制本地 pre-commit `check`** 可替代 PR CI。
- **2.1 新增可操作阈值**:单文件 ~600–800 行(尤其 UI 组件)即复杂度告警信号(原条目缺少 review 抓手)。
- **3.1 新增**:安全/权限边界必须有**专门边界测试**(越权、数据隔离),不能只测 happy path。
- **3.2 新增例外**:单部署目标项目 feature flag 非必须,env 门控 + 可回滚部署即满足"部署 ≠ 发布"实质。

> 方法论:每个真实项目产出"规范的哪条站不住 / 缺什么 / 需要什么例外",而非"项目好不好"。原始审计留私有,只回注泛化规则。

## v0.1 — 2026-06-29

- 基线版本:基于 David Farley《Modern Software Engineering》的开发 + Review 规范。
- 含完整 SPEC、CONTRIBUTING、PR 模板、CC BY 4.0。
